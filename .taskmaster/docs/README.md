# Taskmaster Docs

Use this directory for PRDs and task planning notes that will be parsed into tasks.

Suggested flow:
1. Create a PRD text file in this folder.
2. Parse with `task-master parse-prd --input=<file>`.
3. Refine generated tasks and expand complex items.
